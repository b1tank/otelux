# @otelux/adapter-http

Browser-safe authenticated JSON-RPC/SSE client for an OTelux Runtime API. It implements `DataSource` plus runtime status/settings/sample/clear control methods. Tokens are sent only in `Authorization` headers, never URLs.
