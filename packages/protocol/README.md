# @otelux/protocol

The `DataSource` interface and query/result shapes that connect `@otelux/ui`
to any backend (in-process engine, postMessage bridge, Tauri IPC).

Phase 0 ships a type-level skeleton. Real queries land in Phase 1.
